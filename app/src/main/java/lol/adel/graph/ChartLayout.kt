package lol.adel.graph

import android.content.Context
import android.widget.LinearLayout

fun makeChartLayout(ctx: Context): ViewHolder {
    val root = LinearLayout(ctx).apply {
        orientation = LinearLayout.VERTICAL
        clipChildren = false
    }

    return TODO()
}
